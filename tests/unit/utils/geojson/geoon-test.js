import geojsonGeoon from 'dummy/utils/geojson/geoon';
import { module, test } from 'qunit';

module('Unit | Utility | geojson/geoon', function () {
    // TODO: Replace this with your real tests.
    test('it works', function (assert) {
        let result = geojsonGeoon();
        assert.ok(result);
    });
});
